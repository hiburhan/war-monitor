{
  "name": "war-monitor",
  "version": "1.0.0",
  "description": "Real-time global conflict intelligence dashboard",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
